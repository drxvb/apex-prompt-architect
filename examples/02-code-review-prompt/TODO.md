# Example 02 — code-review prompt

**Status:** stub. Full worked example pending in v1.1.

## What this example should demonstrate
- D2 catches: no severity tiering, no output format, no language-scoping (JS? Python? all?)
- D3 should add: severity tiers (🔴/🟡/🟢), line-number anchoring, anti-rewrite boundary, security-checklist injection for sensitive files
- Three platform variants show: Claude (XML + verification), Kimi (numbered review categories), MiniMax (compact severity-tier list)

## To complete this example
1. Write the three OUTPUT-<platform>.md files matching the structure of example 01
2. Write apex-process.md showing D1–D4 walkthrough
